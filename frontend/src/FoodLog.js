import React from "react";
import {Header} from "semantic-ui-react";


export default function FoodLog(){
    return(
        <Header className={'App-header'} color='white' content={'Welcome to the Food Logger'}/>
    );
}