import React from "react";
import {Header} from "semantic-ui-react";


export default function Sleep(){
    return(
        <Header className={'App-header'} color='white' content={'Welcome to the Sleep (like a) Logger...'}/>
    );
}